name: Daily Data Factory

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Execute Trends Engine
        run: python scripts/fetch_trends.py

      - name: Execute Trust URL Engine
        run: python scripts/fetch_trust_urls.py

      - name: Commit and Push Data to Automation Branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git switch -C automation/daily-data
          git add data/keywords/
          git add data/regions/
          if git diff --staged --quiet; then
            echo "No changes, skipping."
            exit 0
          fi
          git commit -m "chore(data): refresh daily data set [$(date +'%Y-%m-%d')]"
          git push --force-with-lease origin automation/daily-data
